<% if signed_in? %>

  <h4>Take a moment, rate the goods and let us know what you think!</h4>

  <%= form_for ([@product, @product.comments.build]) do |f| %>

      <p>
        <%= f.label :body, "Comments" %><br>
        <%= f.text_area :body %>
      </p>

      <div class="rating"></div>

      <p>
        <%= f.submit "Submit" %>
      </p>

  <% end %>
<% end %>
